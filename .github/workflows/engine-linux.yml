name: Build Linux engine

on:
  push:
    branches: [linux-implicit-view-teardown]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup
          df -h /

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install depot_tools
        run: |
          git clone --depth=1 https://chromium.googlesource.com/chromium/tools/depot_tools.git "$HOME/depot_tools"
          echo "$HOME/depot_tools" >> "$GITHUB_PATH"

      - name: gclient sync
        run: |
          cat > .gclient <<EOF
          solutions = [
            {
              "name": ".",
              "url": "https://github.com/${{ github.repository }}.git@${{ github.sha }}",
              "deps_file": "DEPS",
              "managed": False,
              "custom_vars": {"download_emsdk": False},
            },
          ]
          EOF
          gclient sync --no-history -j4

      - name: Install Linux deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev ninja-build clang

      - name: Build
        run: |
          cd engine/src
          flutter/tools/gn --no-goma --runtime-mode=debug --target-dir host_debug
          ninja -C out/host_debug flutter/shell/platform/linux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: host_debug-linux-x64
          path: |
            engine/src/out/host_debug/libflutter_linux_gtk.so
            engine/src/out/host_debug/libflutter_engine.so
            engine/src/out/host_debug/icudtl.dat
          if-no-files-found: error
